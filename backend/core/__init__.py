"""
Core business logic — tracing, identification, and confidence scoring.

This package implements the investigative engine: following money forward
through the public chain (tracer), deciding whether a wallet belongs to a
regulated entity (identify), and quantifying how much weight that claim
deserves (scoring).
"""
