# Secrets — API keys live only in backend/.env
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
venv/
.venv/
env/
*.egg-info/

# Node / frontend
node_modules/
dist/
.vite/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Recorded demo traces (data/cache/) are deliberately NOT ignored. They are what
# makes the demo work instantly on any machine without a live API call, so a
# teammate who clones the repo gets a working demo. ~1 MB total.
# Uncomment to exclude them:
# data/cache/
